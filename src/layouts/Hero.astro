---
import CurvedLinks from '@/components/CurvedLinks.astro';
import { Link } from '@/components/ui/Link';
import { HoverBorderGradient } from '@/components/ui/HoverBorderGradient';
import { Circle } from '@/icons/Circle';
import { MoveDown } from 'lucide-react';
import { t } from '@/i18n/utils';
import type { Locale } from '@/i18n/config';
import Age from '@/components/Age';

interface Props {
	locale: Locale;
}

const { locale } = Astro.props;
---

<div id="home" class="h-[80dvh] md:h-[90dvh] flex flex-col items-center justify-between scroll-mt-24">
	<p class="text-background font-bold">{t(locale, 'hero.easterEgg')}</p>
	<div class='center gap-8 md:mt-24'>
		<div class='max-w-xl center gap-2'>
			<h1 class='w-fit'>
				<div class='relative inline-block'>
					<span class="absolute grid place-content-center top-0 -left-3 h-full w-full">
						<Circle
							size={150}
							className="*:stroke-orange-500"
							client:load
						/>
					</span>
					{t(locale, 'hero.title')}
				</div>
				<Age birthDate="2003-08-13" client:load /> {t(locale, 'common.yearsOld')}, {t(locale, 'common.apprentice')}
			</h1>
			<p>
				{t(locale, 'hero.description')}
			</p>
		</div>
		<CurvedLinks />
		<div class='center gap-2'>
			<HoverBorderGradient
				as='span'
				containerClassName='rounded-full'
				className='px-4 py-2'
				client:load
			>
				{t(locale, 'hero.currently')}
			</HoverBorderGradient>
			<span>
				{t(locale, 'hero.student')} 
					<Link href='https://www.abtasty.com/'>
						AB Tasty
					</Link>
			</span>
		</div>
	</div>
	<MoveDown className='animate-bounce' />
</div>
